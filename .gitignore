# Static site. app.js is built from keenelandbetlog.jsx but is committed, so
# the repo needs no build step to deploy. These entries just keep local
# tooling and OS junk out of the repo.

# Claude Code: keep launch.json (tracked), ignore machine-local settings
.claude/settings.local.json

# editors
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# logs / scratch
*.log
